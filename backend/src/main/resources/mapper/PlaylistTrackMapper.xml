<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soundprint.mapper.PlaylistTrackMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.soundprint.entity.PlaylistTrack">
        <id column="playlist_id" property="playlistId" />
        <id column="track_id" property="trackId" />
        <result column="order_index" property="orderIndex" />
        <result column="added_at" property="addedAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        playlist_id, track_id, order_index, added_at
    </sql>

</mapper>
